# Copy to .env to override defaults. All values are optional.

# Path to the SQLite database file.
PULSE_DB_PATH=pulse.db

# Host and port for the server (used only as a reference for the simulator).
PULSE_HOST=127.0.0.1
PULSE_PORT=8000
